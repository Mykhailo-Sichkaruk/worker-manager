# ENVIRONMENT: Defines the runtime environment of the application. Options are development, production, test, or staging.
ENVIRONMENT=development

# RABBITMQ_URL: The URL for connecting to the RabbitMQ server, including credentials and port. Format is amqp://username:password@hostname:port.
RABBITMQ_URL=amqp://user:password@localhost:5672

# APP_HOST: The hostname where the application server is accessible. Typically set to localhost for development environments.
APP_HOST=localhost

# APP_PORT: The network port on which the application server listens. Common development ports are 3000 or 8080.
APP_PORT=3000

# API_PREFIX: The initial path prefix under which all API endpoints are located. For example, using '/api' would make accessible endpoints like '/api/users'.
API_PREFIX=/api

# API_VERSION: A version identifier for the API, usually a number or 'v' followed by a number, e.g., '1.0' or 'v1'. It's part of the URL path to ensure API versioning.
API_VERSION=1.0

# API_HOST: The full URL where the API is hosted. It combines APP_HOST and APP_PORT, used externally to reach your API.
API_HOST=http://localhost:3000

# LOGS_PATH: The directory path where application logs are stored. Ensure this directory is writable by the application.
LOGS_PATH=/var/log/myapp

# COOKIE_SECRET: A secret key used for signing and/or encrypting cookies to maintain session security. Should be a long, random and unique string.
COOKIE_SECRET=verysecretkey
